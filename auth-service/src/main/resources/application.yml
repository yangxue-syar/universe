server:
  port: 9101
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/system_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 87654321
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
        # 确保地址正确
  #        username: nacos              # 如有需要，添加认证信息
  #        password: nacos
  mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      global-config:
          db-config:
              id-type: auto
  logging:
  level:
    com.universe:INFO


